# Local-only project files — kept in the working tree for context and tooling,
# but never pushed to GitHub Pages.

# Project notes for Claude (loaded at session start)
CLAUDE.md
ENHANCEMENTS.md
HABIT-REWORK-SPEC.md
BROFESSOR-APP-SPEC.md
.claude/

# Local validator + skill (developer-only)
sync-check.js
finance-tool-builder.skill

# User-facing docs that don't belong on the public site
Beast_Mode_Quick_Start_Guide.docx

# Unrelated standalone tools that share this folder
fitness-twitter-eval-review.html
workout-nutrition-tool.html

# Backup files created by editors / scripts
*.bak*
*.tmp_*
*.tmp

# Cloudflare worker local state and any key that lands here by accident
worker/.wrangler/
worker/node_modules/
*vapid*private*

# Design previews and scratch artwork (galleries at the repo root served for
# review, the recolouring workspace). The brand masters live in the OneDrive
# hub's design/brand/, never here.
/_*.html
/_marks/

# The iPhone app: packages, the copied web files, and Xcode build output
native/node_modules/
native/www/
native/ios/App/Pods/
native/ios/App/build/
native/ios/DerivedData/
